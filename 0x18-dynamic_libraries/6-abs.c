#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs - prints absolute value of an interger
 * @n: input integer
 * Return: absolute value of n
 */
int _abs(int n)
{
	return (abs(n));
}
