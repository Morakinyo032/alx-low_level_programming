#include <stdio.h>
#include <stdlib.h>

/**
 * _memcpy - Copies the content of a memory space to another
 * @src: Pointer to the memory to be copied
 * @des: Pointer to where to copy to
 * @size: size of bytes to copy
 * Return: des
 */
void *_memcpy(void *src, void *des, size_t size)
{
	des = malloc(size);
	if des
}

