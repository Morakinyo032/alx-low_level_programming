#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _strcmp - compares two string
 * @s1: first input
 * @s2: second input
 * Return: integer
 */
int _strcmp(char *s1, char *s2)
{
	int s = strcmp(s1, s2);

	return (s);
}
